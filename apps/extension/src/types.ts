export interface CaptureData {
  url: string;
  selectedText?: string;
  pageTitle?: string;
}

export interface CaptureData {
  url: string;
  selectedText?: string;
  selectedHtml?: string;
  pageTitle?: string;
}